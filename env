import os

EMAIL_MODE = os.getenv("EMAIL_MODE", "smtp")

def send_email(to_email, subject, body):
    if EMAIL_MODE == "mock":
        print("MOCK EMAIL:", to_email, subject)
        return

    # real SMTP (မသုံးတော့ရင် ignore)
