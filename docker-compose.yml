version: "3.9"

services:
  api:
    build: .
    container_name: business-os-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: always

  redis:
    image: redis:7
    container_name: business-os-redis
    ports:
      - "6379:6379"
    restart: always
